name: Test and Build Code

on:
    pull_request:
        branches:
            - develop

jobs:
    build_client:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - name: Install Dependencies
              run: npm install
            - name: Build
              run: npm run build
            - name: Test
              run: npm run test
