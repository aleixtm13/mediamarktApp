import {combineReducers} from 'redux'

// import productReducer from '../reducers/productReducer' //TODO: uncomment when productReducer is created

const rootReducer = combineReducers({})

export default rootReducer

